# SQL Files

These files contain the SQL work organised by topic.

The SQL itself has been kept as-is; the files are simply grouped and named so the repository is easier to browse.

1. `01-querying-and-filtering.sql`
2. `02-joins-and-aggregates.sql`
3. `03-subqueries-and-advanced-sql.sql`
4. `04-database-design-and-ddl.sql`
5. `05-transactions-and-views.sql`

`.sql` is the appropriate extension because these are SQL source files, not plain notes. GitHub will also provide SQL syntax highlighting automatically.

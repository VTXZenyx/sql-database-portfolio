# MusicDB

`MusicDB.db` is the course-provided SQLite database I used for the music-database query work.

I used **DB Browser for SQLite** to open the database and execute SQL.

The database file itself is provided here so the queries can be tested; it is not presented as a database I created.

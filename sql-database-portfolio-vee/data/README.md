# Database File

`MusicDB.db` is the SQLite database used by several of the SQL query examples in this portfolio.

To open it:

1. Open DB Browser for SQLite.
2. Select **Open Database**.
3. Choose `MusicDB.db`.
4. Use the **Execute SQL** tab to run queries from the `sql` folder.
